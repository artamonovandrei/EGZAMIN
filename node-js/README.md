# Dino Dash

Однопользовательская браузерная игра в духе Chrome Dino. Нажимайте `Пробел`, `↑` или клик для прыжка; удерживайте `↓`, чтобы пригнуться. Рекорд сохраняется в браузере.

## Запуск через Docker

```powershell
docker compose up --build -d
```

Затем откройте `http://localhost:3000`.

Остановить контейнер:

```powershell
docker compose down
```
